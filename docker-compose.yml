version: '3.8'

services:
  f1-api-db:
    image: postgres
    ports:
      - '5433:5433'
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
